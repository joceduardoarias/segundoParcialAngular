import { UsuarioLogueadoDirective } from './usuario-logueado.directive';

describe('UsuarioLogueadoDirective', () => {
  it('should create an instance', () => {
    const directive = new UsuarioLogueadoDirective();
    expect(directive).toBeTruthy();
  });
});
