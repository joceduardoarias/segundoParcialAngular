import { FiltrarUsuarioPipe } from './filtrar-usuario.pipe';

describe('FiltrarUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
