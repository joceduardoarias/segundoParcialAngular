import { NoAprobadosPipe } from './no-aprobados.pipe';

describe('NoAprobadosPipe', () => {
  it('create an instance', () => {
    const pipe = new NoAprobadosPipe();
    expect(pipe).toBeTruthy();
  });
});
